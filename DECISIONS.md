# Architecture & trade-offs

- pnpm workspaces keep the React app, Express API, and shared contracts in one TypeScript repository. The contracts package exports Zod schemas and inferred types for health, authentication, public discovery, catalogue queries, book summaries and details, genres, pagination, catalogue errors, and archive visibility policy so runtime and compile-time API definitions stay aligned.
- PostgreSQL persistence uses Prisma migrations and explicit soft-archive/removal fields. The deterministic, idempotent seed creates 240 books, 12 genres, and reader/librarian demo users without network dependencies.
- Public discovery is a dedicated narrow database projection. It queries only active books, limits results to six, orders by `createdAt` descending and then `id` ascending, filters archived genres, and maps associations to display-only genre names before validating the strict response contract.
- Authentication uses scrypt password hashes and a short-lived signed token stored in an HTTP-only, same-site cookie. Roles come from server-side user records, CORS accepts only the configured origin for browser requests, and centralized error handling keeps error responses consistent.
- `app.ts` is the API composition root for global middleware, router mounting, not-found handling, and centralized errors. Health, authentication, discovery, and catalogue use feature-oriented router factories with narrow store interfaces so route ownership and dependencies stay explicit.
- The authenticated catalogue separates HTTP policy from narrow Prisma stores. Reader-facing list, detail, and genre queries use active-only projections; search targets title and author only; every supported sort has an ascending book-ID tie-breaker; and archived and unknown detail IDs share the same `404` response. Catalogue responses are `private, no-store`, while the query contract caps `page` at 10,000 and `pageSize` at 48 to keep offset pagination inputs practical.
- Docker Compose remains the primary local environment: PostgreSQL is health-checked, the API generates its Prisma client during image construction, applies migrations on startup, and exposes a database-backed health check before the web service starts. Seeding remains an explicit repeatable command rather than an automatic startup mutation.
- Preferences, reading lists, librarian management, Swagger/OpenAPI, and the product frontend remain deliberately deferred. The Library Card Chaos design-system document defines implementable visual, responsive, and accessibility guidance without claiming that those React screens exist.

# AI usage

- Codex implemented the shared public-discovery and authenticated-catalogue contracts, narrow Prisma projections, feature routers, focused API tests, API image build-order correction, and milestone documentation. Codex also produced the Library Card Chaos design-system document for the future frontend implementation.
- Codex verified the completed API work with workspace typechecking, 51 passing tests across six files, a production build, Compose configuration validation and rebuild, and live requests to health, public discovery, login, authenticated book list and detail, and active genres. The Docker build exposed the Prisma generation-order issue; the final Dockerfile reflects the verified correction.
