generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Domain models are intentionally deferred. This scaffold only establishes
// PostgreSQL connectivity, migration mechanics, and an idempotent seed hook.
